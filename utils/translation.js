import LocalizedStrings from 'react-native-localization';

import fr from '../assets/translations/fr.js'
import en from '../assets/translations/en.js'

let strings = new LocalizedStrings({
    fr: fr,
    en: en
});

export default strings;
